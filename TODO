- General
 * Create an admin section
 * Create an installer

- Data Loaders
 * Incorporate the loader into the application in an admin section
 * Make the zoom level calculator part of addWpt()
 * A load should clear the cache
 * GTFS Loader - if agency is "OC Transpo" add link to stop info.

- Data Extractor
 * Re-write the dump utility using the new classes

- Database
 * Add a table called 'dataset' and associate it with poi. The table 
would store name, load data, source of data, and license information.

- Translation
 * l10n, i18n, and then translate to French.

- AJAX
 * Validate the data returned by api.php.

- Documentation
 * Write README and INSTALL
