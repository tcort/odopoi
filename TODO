- AJAX Marker Loading
 * Only remove markers that are off the screen.
 * Only add markers that aren't already on the screen.
 * As the user zooms out, display fewer markers so that it doesn't look 
like a giant blob.

- Data Loaders
 * Write them as the data becomes available.

- Graphics Improvements
 * Get someone with mad skillz to design the icons.
 * Scale icons depending on zoom level.

- GPS Export
 * Write a script that dumps the database to a GPX file.
