- Translation
 * l10n, i18n, and then translate to French.

- Frontend
 * geoip/geolocation to determine where the map should be centered on the user's first visit
 * Add link to GPX for the current map
 * Don't fetch another GPX file if a fetch is in progress.
 * Display type of point in info bubble
 * Permalinks
 * Upgrade OpenLayers to 2.10
  + Allow the map to be dragged even when an icon has been clicked.

- posm_extractor
 * write a "point in polygon" function
 * create a polygon for canada
 * add support for applying changesets
 * parallelize

- General
 * add "office" and "craft" keys
